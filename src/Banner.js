import React, { useState } from "react";

function Banner() {
  const [movie, setMovie] = useState([]);
  return (
    <header>
      {/*<<<background image*/}
      {/* title */}
      {/* div->2 buttons */}
      {/* description */}
    </header>
  );
}

export default Banner;
